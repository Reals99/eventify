{
  "name": "eventify-server",
  "version": "1.0.0",
  "description": "Eventify backend — Node.js + Express",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.1",
    "multer": "^1.4.5-lts.1"
  },
  "optionalDependencies": {
    "ffmpeg-static": "^5.2.0"
  },
  "notes": {
    "ffmpeg": "FFmpeg is used via child_process spawn. It must be installed on the system. On Render.com it is pre-installed. Locally: https://ffmpeg.org/download.html"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
